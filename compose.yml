services:
  db:
    image: postgres:18-alpine
    container_name: hr-sat-dev
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ardy1607
      POSTGRES_DB: hr_sat
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data:
