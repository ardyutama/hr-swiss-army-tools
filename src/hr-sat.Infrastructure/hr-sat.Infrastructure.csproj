<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>hr_sat.Infrastructure</RootNamespace>
    <AssemblyName>hr-sat.Infrastructure</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="MailKit" />
    <PackageReference Include="Microsoft.AspNetCore.DataProtection" />
    <PackageReference Include="Microsoft.EntityFrameworkCore" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.Design">
      <PrivateAssets>all</PrivateAssets>
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
    </PackageReference>
    <PackageReference Include="Microsoft.Extensions.Hosting.Abstractions" />
    <!-- MSAL.NET (issue 03, decision 3): device-code acquisition and silent refresh,
         no hand-rolled OAuth2 — polling, slow_down, rotation races, and expiry math
         are where homegrown auth rots. Rationale in ADR-0020. -->
    <PackageReference Include="Microsoft.Identity.Client" />
    <PackageReference Include="Npgsql.EntityFrameworkCore.PostgreSQL" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\hr-sat.Domain\hr-sat.Domain.csproj" />
    <ProjectReference Include="..\hr-sat.Application\hr-sat.Application.csproj" />
  </ItemGroup>

  <ItemGroup>
    <None Include="Infrastructure\Seed\Fixtures\**\*"
          CopyToOutputDirectory="PreserveNewest"
          CopyToPublishDirectory="PreserveNewest" />
  </ItemGroup>

</Project>