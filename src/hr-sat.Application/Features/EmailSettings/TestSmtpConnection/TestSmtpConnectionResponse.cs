namespace hr_sat.Application.Features.EmailSettings.TestSmtpConnection;

// The passing probe's copy, server-owned so the page and any future surface agree
// (layout doc section C: "Connected and authenticated.").
public sealed record TestSmtpConnectionResponse(string Message);
